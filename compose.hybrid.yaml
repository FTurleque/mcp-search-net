# Overlay hôte : publie uniquement les providers sur loopback pour le serveur
# Node/IntelliJ. Le mode conteneur utilise compose.yaml seul et reste interne.
services:
  searxng:
    ports:
      - '127.0.0.1:8888:8080'
  crawl4ai:
    ports:
      - '127.0.0.1:11235:11235'
