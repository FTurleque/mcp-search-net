# Conservé pour compatibilité avec les commandes historiques. Les publications
# loopback sûres sont désormais dans compose.yaml afin que `docker compose up`
# fonctionne directement depuis un clone propre.
services: {}
